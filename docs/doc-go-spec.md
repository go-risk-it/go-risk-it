# Doc.go Specification

Living architecture documentation for go-risk-it. Every non-excluded package
gets a `doc.go` file that embeds its architectural role directly in the source.

## Layer Taxonomy

Each package belongs to exactly one layer. The layer determines import rules,
testing expectations, and the package's role in the dependency graph.

| Layer | Description | Import constraints |
|-------|-------------|-------------------|
| `api` | Data transfer objects (request/response/messaging types) | May only import other `api/` packages |
| `config` | Application configuration | Leaf package — no internal imports |
| `ctx` | Request context types | Leaf-like — minimal internal imports |
| `data` | Database access and persistence | Never imports `logic/` or `web/` |
| `events` | Event bus infrastructure | Never imports `logic/` or `web/` |
| `events-domain` | Domain event payload types | May import `logic/` types for payloads |
| `logic` | Business rules and domain services | Never imports `web/` or `api/` |
| `shared` | Cross-cutting domain types (errors) | Imported by all layers |
| `infrastructure` | Pure utilities with no internal deps | Leaf packages — no internal imports |
| `web` | HTTP/WS handlers, middleware, routing | May import all other layers |
| `test` | Test-only packages | Never imported by production code |
| `wiring` | fx Module aggregation roots | One file, fx.Module only |

## Package Classification

### Exclusions (no doc.go required)

These packages are excluded from doc.go requirements entirely:

| Package | Reason |
|---------|--------|
| `internal` (root) | Wiring root — single `internal.go` with fx.Module |
| `*/sqlc` | Auto-generated by sqlc |
| `*/mocks` | Auto-generated by mockery |

### Wiring roots (no doc.go required)

Single-file packages whose sole purpose is fx.Module aggregation:

| Package | File |
|---------|------|
| `logic` | `logic.go` |
| `logic/game` | `game.go` |
| `logic/game/move` | `move.go` (only has `service.go`, but 1 GoFile = wiring) |
| `logic/lobby` | `lobby.go` |
| `data` | `data.go` |
| `data/game` | `game.go` |
| `data/lobby` | `lobby.go` |
| `web` | `web.go` |
| `logic/game/move/service` | `service.go` |
| `web/game` | `game.go` |
| `web/lobby` | `lobby.go` |

Detection rule: `len(GoFiles) == 1` AND the single file is named after the
last path segment (e.g., `logic/game/game.go`).

### Full-tier packages

Packages that define a service boundary: has `service.go` AND `len(GoFiles) > 2`
AND is not a wiring root or generated package.

Required doc.go sections:
1. Package summary (first paragraph)
2. `# Layer` — layer name from taxonomy
3. `# Key Types` — exported interfaces and key structs
4. `# Dependencies` — key service dependencies

| Package | Layer | GoFiles |
|---------|-------|---------|
| `logic/game/board` | logic | 5 |
| `logic/game/mission` | logic | 3 |
| `logic/game/move/attack` | logic | 4 |
| `logic/game/move/cards` | logic | 4 |
| `logic/game/move/conquer` | logic | 4 |
| `logic/game/move/deploy` | logic | 4 |
| `logic/game/move/reinforce` | logic | 4 |
| `logic/game/phase` | logic | 3 |
| `logic/game/player` | logic | 3 |
| `testonly` | test | 5 |

### Lightweight-tier packages

All other non-excluded packages. Required doc.go sections:
1. Package summary (first paragraph)
2. `# Layer` — layer name from taxonomy

| Package | Layer |
|---------|-------|
| `api/game` | api |
| `api/game/messaging` | api |
| `api/game/rest/request` | api |
| `api/game/rest/response` | api |
| `api/lobby/messaging` | api |
| `api/lobby/rest/request` | api |
| `api/lobby/rest/response` | api |
| `config` | infrastructure |
| `ctx` | ctx |
| `data/db` | data |
| `data/game/db` | data |
| `data/lobby/db` | data |
| `data/migration` | data |
| `data/pool` | data |
| `events` | events |
| `events/game` | events-domain |
| `events/lobby` | events-domain |
| `events/logger` | events |
| `logic/errors` | shared |
| `logic/game/advancement` | logic |
| `logic/game/card` | logic |
| `logic/game/creation` | logic |
| `logic/game/headlines` | logic |
| `logic/game/mission/checker` | logic |
| `logic/game/move/attack/dice` | logic |
| `logic/game/move/attack/dice/roller` | logic |
| `logic/game/move/orchestration` | logic |
| `logic/game/move/orchestration/logging` | logic |
| `logic/game/move/orchestration/validation` | logic |
| `logic/game/move/validation` | logic |
| `logic/game/region` | logic |
| `logic/game/region/assignment` | logic |
| `logic/game/snapshot` | logic |
| `logic/game/state` | logic |
| `logic/game/timing` | logic |
| `logic/lobby/creation` | logic |
| `logic/lobby/management` | logic |
| `logic/lobby/start` | logic |
| `logic/lobby/state` | logic |
| `metrics` | infrastructure |
| `rand` | infrastructure |
| `slog` | infrastructure |
| `testing/invariant` | test |
| `testonly` | test |
| `tracing` | infrastructure |
| `upgradablerw_mutex` | infrastructure |
| `web/game/controller` | web |
| `web/game/converter` | web |
| `web/game/publisher` | web |
| `web/game/rest` | web |
| `web/game/ws` | web |
| `web/lobby/controller` | web |
| `web/lobby/publisher` | web |
| `web/lobby/rest` | web |
| `web/lobby/ws` | web |
| `web/middleware` | web |
| `web/mux` | web |
| `web/nbio` | web |
| `web/otel` | web |
| `web/rest` | web |
| `web/rest/health` | web |
| `web/rest/route` | web |
| `web/rest/utils` | web |
| `web/ws` | web |
| `web/ws/message` | web |

Note: Packages with `service.go` and 2 or fewer non-test GoFiles are
classified as lightweight (e.g., `logic/game/move/orchestration` has 2 files).
The `logic/game/move/service` package has only 1 file and is classified as a
wiring root.

## Templates

### Full-tier template

```go
// Package <name> <one-line summary>.
//
// <Expanded description — what problem this package solves, what invariants
// it maintains, and how it fits into the game engine.>
//
// # Layer
//
// Logic — business rules for <domain concept>.
//
// # Key Types
//
// [Service] is the primary interface. It provides methods for <operations>.
//
// [serviceImpl] implements [Service] using <dependencies>.
//
// # Dependencies
//
//   - [region.Service] for territory ownership queries
//   - [player.Service] for active player lookups
//   - [db.Querier] for database access within transactions
package <name>
```

### Lightweight-tier template

```go
// Package <name> <one-line summary>.
//
// <Optional expanded description.>
//
// # Layer
//
// Infrastructure — <role description>.
package <name>
```

## Tier Assignment Algorithm

Given a package `P` with import path `github.com/go-risk-it/go-risk-it/internal/<suffix>`:

```
1. If suffix contains "/sqlc" or "/mocks" → EXCLUDED
2. If suffix == "" (internal root) → EXCLUDED
3. If len(GoFiles) == 1 AND GoFiles[0] matches "<lastSegment>.go" → WIRING (excluded)
4. If "service.go" in GoFiles AND len(non-test GoFiles) > 2 → FULL
5. Otherwise → LIGHTWEIGHT
```

## Layer Assignment Algorithm

The layer is determined by the package path prefix:

```
1. starts with "api/" → api
2. == "config" → infrastructure
3. == "ctx" → ctx
4. starts with "data/" → data
5. == "events" or == "events/logger" → events
6. starts with "events/" (game/lobby) → events-domain
7. == "logic/errors" → shared
8. starts with "logic/" → logic
9. == "metrics" or == "rand" or == "slog" or == "tracing"
   or == "upgradablerw_mutex" → infrastructure
10. starts with "testing/" or == "testonly" → test
11. starts with "web/" → web
```

## Validation Rules (enforced by arch_test.go)

1. **TestArch_DocGoExists**: Count of packages with doc.go >= `minDocGoCount` baseline
2. **TestArch_DocGoSections**: Full-tier packages must have `# Layer`, `# Key Types`,
   `# Dependencies` headings. Lightweight must have `# Layer`.
3. **TestArch_DocGoLayer**: The paragraph after `# Layer` must start with the
   expected layer name for that package.

All rules only check packages that already HAVE a doc.go — they enforce
correctness of existing files, not existence of new ones. Existence is
ratcheted via the baseline count.
